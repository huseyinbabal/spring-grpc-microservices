# NodePort 30000 → host :3000.
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - { name: http, port: 3000, targetPort: http, nodePort: 30000 }
